import React from 'react';
import Button from '@joern/button';
import Tag from '@joern/tag';

const Template = () => {
  return (
    <div>
      <Button type={'default'}>1234</Button>
      <Tag title={'321'} />
    </div>
  );
};

export default Template;
